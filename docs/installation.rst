.. highlight:: shell

============
Installation
============

At the command line::

    $ pip install --user pynba

Or, with virtualenvwrapper installed::

    $ mkvirtualenv pynba
    $ pip install pynba
