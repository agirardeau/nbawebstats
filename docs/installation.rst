.. highlight:: shell

============
Installation
============

At the command line::

    $ easy_install pynba

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv pynba
    $ pip install pynba
